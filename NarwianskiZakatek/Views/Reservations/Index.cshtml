@model NarwianskiZakatek.Services.PaginatedList<NarwianskiZakatek.Models.Reservation>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<h1>Lista rezerwacji</h1>
<partial name="_StatusMessage" />

<form asp-action="Index" method="get">
    <partial name="_ReservationFilter" />
    <div class="row">
        <div class="col-md-4">
            <p>
                <input type="submit" value="Filtruj" style="margin: 5px" class="btn btn-primary" /> <a asp-action="Index">Wyczyść filtry</a>
            </p>
        </div>
        <div class="col-md-8">
        </div>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @if (ViewBag.SortOrder == "begin")
                {
                    <a asp-action="Index" asp-route-pageNumber="@(Model.PageIndex)" asp-route-sortOrder="begin_desc" asp-route-beginFrom="@ViewBag.BeginFrom" asp-route-beginTo="@ViewBag.BeginTo" asp-route-endFrom="@ViewBag.EndFrom" asp-route-endTo="@ViewBag.EndTo" asp-route-priceFrom="@ViewBag.PriceFrom" asp-route-priceTo="@ViewBag.PriceTo">
                        Data rozpoczęcia
                    </a>
                }
                else
                {
                    <a asp-action="Index" asp-route-pageNumber="@(Model.PageIndex)" asp-route-sortOrder="begin" asp-route-beginFrom="@ViewBag.BeginFrom" asp-route-beginTo="@ViewBag.BeginTo" asp-route-endFrom="@ViewBag.EndFrom" asp-route-endTo="@ViewBag.EndTo" asp-route-priceFrom="@ViewBag.PriceFrom" asp-route-priceTo="@ViewBag.PriceTo">
                        Data rozpoczęcia
                    </a>
                }
            </th>
            <th>
                @if (ViewBag.SortOrder == "end")
                {
                    <a asp-action="Index" asp-route-pageNumber="@(Model.PageIndex)" asp-route-sortOrder="end_desc" asp-route-beginFrom="@ViewBag.BeginFrom" asp-route-beginTo="@ViewBag.BeginTo" asp-route-endFrom="@ViewBag.EndFrom" asp-route-endTo="@ViewBag.EndTo" asp-route-priceFrom="@ViewBag.PriceFrom" asp-route-priceTo="@ViewBag.PriceTo">
                        Data zakończenia
                    </a>
                }
                else
                {
                    <a asp-action="Index" asp-route-pageNumber="@(Model.PageIndex)" asp-route-sortOrder="end" asp-route-beginFrom="@ViewBag.BeginFrom" asp-route-beginTo="@ViewBag.BeginTo" asp-route-endFrom="@ViewBag.EndFrom" asp-route-endTo="@ViewBag.EndTo" asp-route-priceFrom="@ViewBag.PriceFrom" asp-route-priceTo="@ViewBag.PriceTo">
                        Data zakończenia
                    </a>
                }
            </th>
            <th>
                @if (ViewBag.SortOrder == "price")
                {
                    <a asp-action="Index" asp-route-pageNumber="@(Model.PageIndex)" asp-route-sortOrder="price_desc" asp-route-beginFrom="@ViewBag.BeginFrom" asp-route-beginTo="@ViewBag.BeginTo" asp-route-endFrom="@ViewBag.EndFrom" asp-route-endTo="@ViewBag.EndTo" asp-route-priceFrom="@ViewBag.PriceFrom" asp-route-priceTo="@ViewBag.PriceTo">
                        Cena(zł)
                    </a>
                }
                else
                {
                    <a asp-action="Index" asp-route-pageNumber="@(Model.PageIndex)" asp-route-sortOrder="price" asp-route-beginFrom="@ViewBag.BeginFrom" asp-route-beginTo="@ViewBag.BeginTo" asp-route-endFrom="@ViewBag.EndFrom" asp-route-endTo="@ViewBag.EndTo" asp-route-priceFrom="@ViewBag.PriceFrom" asp-route-priceTo="@ViewBag.PriceTo">
                        Cena(zł)
                    </a>
                }
            </th>
            <th>
                Właściciel rezerwacji
            </th>
            <th>
                Status rezerwacji
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.BeginDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EndDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.UserName)
                </td>
                <td>
                    @if(item.IsCancelled)
                    {
                        <text>Anulowana</text>
                    }
                    else if(item.BeginDate > DateTime.Today)
                    {
                        <text>Złożona</text>
                    }
                    else if(item.EndDate >= DateTime.Today)
                    {
                        <text>W trakcie</text>
                    }
                    else if(item.Opinion == null)
                    {
                        <text>Zakończona</text>
                    }
                    else
                    {
                        <text>Oceniona</text>
                    }
                </td>
                <td>
                    <a asp-action="DetailsFull" asp-route-id="@item.ReservationId">Szczegóły rezerwacji</a>
                    @if (item.BeginDate > DateTime.Today)
                    {
                        <text> | </text>
                        <a asp-action="DeleteAny" asp-route-id="@item.ReservationId">Anuluj rezerwację</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

@{
    var prevDisabled = !Model.HasPreviousPage ? "pointer-events: none" : "";
    var nextDisabled = !Model.HasNextPage ? "pointer-events: none" : "";
    var prevClass = !Model.HasPreviousPage ? "btn btn-secondary" : "btn btn-primary";
    var nextClass = !Model.HasNextPage ? "btn btn-secondary" : "btn btn-primary";
}

<a asp-action="Index"
   asp-route-pageNumber="@(Model.PageIndex - 1)"
   class="@prevClass" style="@prevDisabled">
    Poprzednia strona
</a>
<a asp-action="Index"
   asp-route-pageNumber="@(Model.PageIndex + 1)"
   class="@nextClass" style="@nextDisabled">
    Kolejna strona
</a>